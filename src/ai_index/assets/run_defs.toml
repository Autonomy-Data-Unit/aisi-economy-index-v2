[defaults]
years = "2025"
sample_n = 0
sample_seed = 42
embedding_model = "text-embedding-3-large"
cosine_mode = "api"
topk = 5
llm_model = "gpt-5.2"

[runs.baseline]
# inherits all defaults

[runs.test]
sample_n = 10

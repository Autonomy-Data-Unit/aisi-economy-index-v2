# AUTOGENERATED! DO NOT EDIT! File to edit: nbs/ai_index/const.ipynb

__all__ = ['adulib_cache_path', 'adzuna_db_path', 'adzuna_store_path', 'assets_path', 'embed_models_config_path', 'inputs_path', 'llm_models_config_path', 'onet_store_path', 'pipeline_store_path', 'pkg_path', 'run_defs_path', 'store_path']

# %% nbs/ai_index/const.ipynb 2
from importlib.resources import files
from pathlib import Path

pkg_path = Path(str(files("ai_index")))
assets_path = pkg_path / "assets"
store_path = pkg_path / "store"
inputs_path = store_path / "inputs"

llm_models_config_path = assets_path / "llm_models.toml"
embed_models_config_path = assets_path / "embed_models.toml"
adulib_cache_path = store_path / "adulib_cache"

onet_store_path = inputs_path / "onet"
adzuna_db_path = inputs_path / "adzuna.duckdb"
adzuna_store_path = inputs_path / "adzuna"  # legacy parquet store

pipeline_store_path = store_path / "pipeline"
run_defs_path = assets_path / "run_defs.toml"
